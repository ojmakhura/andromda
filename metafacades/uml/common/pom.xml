<project>
    <modelVersion>4.0.0</modelVersion>
    <parent>
        <groupId>org.andromda.metafacades</groupId>
        <artifactId>andromda-uml-metafacades</artifactId>
        <version>3.1-SNAPSHOT</version>
    </parent>
    <artifactId>andromda-metafacades-uml</artifactId>
    <packaging>model</packaging>
    <name>AndroMDA UML Common Metafacades</name>
    <description>The common UML metafacades.  This is the set of core UML metafacades used to provide access UML meta models.</description>
    <dependencies>
        <!-- normally this dependency would be in the dependencies section of the andromda-bootstrap-plugin
             however, for some reason it appears that it doesn't see it for some strange reason -->
        <dependency>
            <groupId>org.andromda.bootstrap.cartridges</groupId>
            <artifactId>andromda-meta-cartridge</artifactId>
            <version>${version}</version>
            <scope>runtime</scope>
        </dependency>
    </dependencies>
    <build>
        <filters>
            <filter>mda.properties</filter>
        </filters>
        <plugins>
            <plugin>
              <groupId>org.andromda.maven.plugins</groupId>
              <artifactId>andromda-bootstrap-plugin</artifactId>
              <executions>
                  <execution>
                      <goals>
                          <goal>run</goal>
                      </goals>
                  </execution>
              </executions>
              <configuration>
                  <buildSourceDirectory>${basedir}/target/src</buildSourceDirectory>
                  <lastModifiedCheck>true</lastModifiedCheck>
              </configuration>
            </plugin>
            <plugin>
                <extensions>true</extensions>
                <groupId>org.andromda.maven.plugins</groupId>
                <artifactId>andromda-model-archiver-plugin</artifactId>
                <executions>
                    <execution>
                        <goals>
                            <goal>xml.zip</goal>
                        </goals>
                    </execution>
                </executions>
                <configuration>
                    <modelSourceDirectory>src/uml</modelSourceDirectory>
                    <generateJar>true</generateJar>
                    <buildSourceDirectory>target/src</buildSourceDirectory>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.andromda.maven.plugins</groupId>
                <artifactId>andromda-bootstrap-install-plugin</artifactId>    
                <executions>
                    <execution>
                        <goals>
                            <goal>install</goal>
                        </goals>
                    </execution>
                </executions> 
                <configuration>
                    <installDirectory>${basedir}/../../../lib</installDirectory>
                </configuration>           
            </plugin>
        </plugins>
        <resources>
            <resource>
                <directory>src/java</directory>
                <excludes>
                    <exclude>**/*.java</exclude>
                </excludes>
            </resource>
            <resource>
                <directory>src</directory>
                <includes>
                    <include>META-INF/**/*</include>
                </includes>
                <excludes>
                    <exclude>META-INF/andromda**/*</exclude>
                </excludes>
            </resource>
            <resource>
                <directory>target/src</directory>
                <excludes>
                    <exclude>**/*.java</exclude>
                </excludes>
            </resource>
        </resources>
    </build>
</project>