import { Component, OnInit } from '@angular/core';

@Component({
  selector: 'app-$stringUtils.lowerCase(${applicationId})-layout',
  templateUrl: './$stringUtils.lowerCase(${applicationId})-layout.component.html',
  styleUrls: ['./$stringUtils.lowerCase(${applicationId})-layout.component.css']
})
export class $stringUtils.capitalize(${applicationId})LayoutComponent implements OnInit {

  constructor() { }

  ngOnInit() {
  }

}