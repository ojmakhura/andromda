import { Field, FieldTree } from '@angular/forms/signals';
// Generated by andromda-angular cartridge (view\components\component.ts.vsl) DO NOT EDIT
import {
  AfterViewInit,
  Component,
  inject, OnDestroy,
  OnInit,
  ViewChild
} from "@angular/core";
import { CommonModule, NgComponentOutlet } from '@angular/common';
import { TranslateModule } from '@ngx-translate/core';
import { MAT_DIALOG_DATA, MatDialogModule, MatDialogRef } from "@angular/material/dialog";
import { GenericComponent } from "@views/generic.component";
import { MatButtonModule } from "@angular/material/button";

@Component({
  selector: "app-dialog",
  templateUrl: "./dialog.html",
  styleUrls: [],
  standalone: true,
  imports: [
    CommonModule,
    TranslateModule,
    MatDialogModule,
    MatButtonModule,
    NgComponentOutlet,
  ]
})
export class DialogComponent<D, T extends GenericComponent<D>> implements OnInit, OnDestroy, AfterViewInit {

  readonly dialogRef = inject(MatDialogRef<DialogComponent<D, T>>);
  readonly data = inject(MAT_DIALOG_DATA);

  @ViewChild(NgComponentOutlet, {static: false}) dialogChild!: NgComponentOutlet<T>;

  constructor() {

  }

  ngOnInit(): void {
  }

  ngOnDestroy(): void {
  }

  ngAfterViewInit(): void {

  }

  save() {
    this.dialogRef.close(this.dialogChild);
  }

  doDialogAction(action: string) {

    const componentInstance = this.dialogChild.componentInstance;

    this.dialogRef.close({
      action: action,
      value: componentInstance?.formSignal()
    });
  }

  close() {
    this.dialogRef.close();
  }
}
