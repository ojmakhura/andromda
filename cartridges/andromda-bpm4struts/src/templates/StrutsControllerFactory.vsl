#set ($generatedFile = "${class.fullPath}Factory.java")
##
##  START TEMPLATE
##
package $class.packageName;

public class ${class.name}Factory
{
    private static final ${class.name} INSTANCE = new ${class.name}();

    public final static ${class.name}Interface get${class.name}Instance()
    {
        return INSTANCE;
    }
}