<project name="andromda-hibernate" default="dist">

    <!-- ============================================================= -->
    <!--               Directory settings                              -->
    <!-- ============================================================= -->

    <property name="src.dir"          value="src" />
    <property name="java.dir"         value="src/java" />
    <property name="lib.dir"          value="lib" />
    <property name="build.dir"        value="build" />
    <property name="classes.dir"      value="build/classes" />
    <property name="dist.dir"         value="dist" />
    <property name="dist.stage.dir"   value="dist/predist" />

    <!-- Setup directory structure -->
    <target name="init">
        <tstamp/>
        <mkdir dir="${dist.dir}"/>
        <mkdir dir="${dist.stage.dir}"/>
        <mkdir dir="${build.dir}"/>
        <mkdir dir="${classes.dir}"/>
    </target>

    <!-- Remove directory structure structure -->
    <target name="clean" description="Removes all generated files">
        <delete includeEmptyDirs="true" quiet="true">
            <fileset dir="${dist.stage.dir}"/>
            <fileset dir="${dist.dir}"/>
            <fileset dir="${classes.dir}"/>
            <fileset dir="${build.dir}"/>
        </delete>
    </target>

    <!-- ============================================================= -->
    <!--               Highlevel user tasks                            -->
    <!-- ============================================================= -->

    <!-- build distribution zip file -->
    <target name="dist" depends="clean,build"
            description="Builds the distribution structure">
      <copy todir="${dist.stage.dir}/" >
          <fileset dir="${build.dir}" includes="*.jar" />
          <fileset dir="." includes="andromda-hibernate.properties" />
          <fileset dir="." includes="build.xml" />
          <fileset dir="." includes=".classpath" />
          <fileset dir="." includes=".project" />
      </copy>
      <copy todir="${dist.stage.dir}/lib" >
          <fileset dir="${lib.dir}" includes="**/*" />
      </copy>
      <copy todir="${dist.stage.dir}/src" >
          <fileset dir="${src.dir}" includes="**/*" />
      </copy>
      <zip zipfile="${dist.dir}/andromda-hibernate.zip">
          <zipfileset dir="${dist.stage.dir}" includes="**/*" prefix="andromda-hibernate" />
      </zip>
    </target>

    <!-- build cartridge jar file only -->
    <target name="build" depends="init"
            description="Builds the objects from the sources">
        <jar jarfile="${build.dir}/andromda-hibernate.jar">
            <fileset dir="${src.dir}">
                <include name="templates/*" />
                <include name="META-INF/*" />
            </fileset>
        </jar>
    </target>

</project>
