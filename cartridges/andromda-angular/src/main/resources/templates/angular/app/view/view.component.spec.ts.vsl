#set ( $componentPath = $stringUtils.replaceChars($stringUtils.lowerCase(${view.packageName}), '\\.', '\\/') )
#set ( $fileName = $stringUtils.replaceChars($stringUtils.lowerCase(${view.name}), '\\ ', '\\-'))
#set ( $className = $stringUtils.replaceChars($stringUtils.capitalize(${view.name}), '\\ ', ''))
import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { ${className}View } from './${fileName}.component';

describe('${className}View', () => {
	let component: ${className}Component;
	let fixture: ComponentFixture<${className}View>;

	beforeEach(async(() => {
		TestBed.configureTestingModule({
			declarations: [${className}View]
		})
			.compileComponents();
	}));

	beforeEach(() => {
		fixture = TestBed.createComponent(${className}View);
		component = fixture.componentInstance;
		fixture.detectChanges();
	});

	it('should create', () => {
		expect(component).toBeTruthy();
	});
});
