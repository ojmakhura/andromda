// Generated by andromda-angular cartridge (usecase\use-case-routing.module.ts.vsl) DO NOT EDIT
#set ($useCase = $controller.useCase)
import { importProvidersFrom } from '@angular/core';
import { Routes, RouterModule } from '@angular/router';

import { ${useCase.componentName}Component } from '@${useCase.path}';

#if($secure.equals("true"))
import { AuthenticationGuard } from '@app/auth/authentication.guard';

#end
#foreach($view in $useCase.views)
import { ${view.componentImplementationName} } from '@${view.implementationFilePath}';
#end
#if($useCase.controller)
#set ($controller = $useCase.controller)
import { ${controller.implementationName} } from ${escTool.s}@${controller.implementationFilePath}${escTool.s};
#foreach($service in $controller.allRestControllers)
import { ${service.name} } from ${escTool.s}@${service.filePath}${escTool.s};
#end
#end
#foreach($controller in $view.targetControllers)
import { ${controller.implementationName} } from ${escTool.s}@${controller.implementationFilePath}${escTool.s};
#end
#set($hasViews = $useCase.views.size() > 0)

export const routes: Routes = [
  { 
      path: '', 
      component: #if($hasViews)${useCase.initialView.componentImplementationName}#else${useCase.componentName}Component#end, 
#if($secure.equals("true"))
      canActivate: [AuthenticationGuard],
#end
      data: { title: #if($hasViews)'${useCase.initialView.titleValue}'#else'${useCase.titleValue}'#end },
      providers: [
#if($useCase.controller)
#set ($controller = $useCase.controller)
        ${useCase.controller.implementationName},
#foreach($service in $controller.allRestControllers)
        ${service.name},
#end
#end
      ] 
  },
#foreach($view in $useCase.views)
#set($isInitialView = $useCase.initialView == $view)
#if(!$isInitialView)
  { 
      path: '${view.routerPath}', 
      component: ${view.componentImplementationName}, 
#if($secure.equals("true"))
      canActivate: [AuthenticationGuard],
#end
      data: { title: '${view.titleValue}' },
      providers: [ 
#foreach($controller in $view.targetControllers)
      $controller.variableName: ${controller.implementationName};
#end   
#if($useCase.controller)
#set ($controller = $useCase.controller)
        ${useCase.controller.implementationName},
#end
      ] 
  },
#end
#end
];

