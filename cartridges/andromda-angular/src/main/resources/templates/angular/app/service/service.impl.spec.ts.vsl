import { TestBed } from '@angular/core/testing';

import { ${service.implementationName} } from '@app/${service.implementationFilePath}';

describe('${service.implementationName}', () => {
  beforeEach(() => TestBed.configureTestingModule({}));

  it('should be created', () => {
    const service: ${service.implementationName} = TestBed.get(${service.implementationName});
    expect(service).toBeTruthy();
  });
});