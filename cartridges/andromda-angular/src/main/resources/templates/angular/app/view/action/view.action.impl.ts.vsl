// Generated by andromda-angular cartridge (view\action\view.action.impl.ts.vsl) CAN BE EDITED
#set ( $actionPath = $action.path )
#set ( $fileName = $angularUtils.getComponentFileName("$action.name") )
#set ( $selectorName = $angularUtils.getSelectorName("$actionPath") )
#set ( $className = $angularUtils.getActionName("$actionPath") )
#set($formName = $action.formBeanName)
import { Component, Injector } from '@angular/core';
import { ${className}Component } from '@app/view${actionPath}.component';

@Component({
  selector: '${selectorName}',
  templateUrl: './${angularUtils.getNameFromPath($action.path)}.component.html',
  styleUrls: ['./${angularUtils.getNameFromPath($action.path)}.component.scss']
})
export class ${className}ImplComponent extends ${className}Component {

  constructor(private injector: Injector) {
      super(injector);
  }

  beforeOnInit(){
  }
	
  afterOnInit() {
  }

  doNgAfterViewInit() {
    
  }

  after$stringUtils.capitalize(${action.triggerName})(#if(!$action.formFields.empty)form: any#end) {    
  }

  do$stringUtils.capitalize($action.triggerName)(#if(!$action.formFields.empty)form: any#end) {
  }
}
