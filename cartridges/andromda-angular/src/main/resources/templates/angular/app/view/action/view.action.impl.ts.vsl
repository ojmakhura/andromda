// Generated by andromda-angular cartridge (view\action\view.action.impl.ts.vsl) DO NOT EDIT! -->
#set ( $actionPath = $action.path )
#set ( $fileName = $angularHelper.getComponentFileName("$action.name") )
#set ( $className = $angularHelper.getComponentName($action.name, "-") )
#set($formName = $action.formBeanName)
import { Component, Injector } from '@angular/core';
import { ${className}Component } from 'src/app/gen/view${actionPath}.component';

@Component({
  selector: '${action.name}',
  templateUrl: './${angularHelper.getNameFromPath($action.path)}.component.html',
  styleUrls: ['./${angularHelper.getNameFromPath($action.path)}.component.css']
})
export class ${className}ImplComponent extends ${className}Component {

  constructor(private injector: Injector) {
      super(injector);
  }

  beforeOnInit(){
  }
	
  afterOnInit() {
  }

  doNgAfterViewInit() {
    
  }

  do$stringUtils.capitalize($action.triggerName)() {
  }
}
