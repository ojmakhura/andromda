#set($destination = $stringUtils.replaceChars(${class.packageName}, "\\.", "\\/"))
#set($fileName = $angularUtils.getComponentFileName(${class.name}))
## Use a different name for VO Fault than for Exception, if needed
##set ($className = ${class.getName(true)})
#if ($class.hasStereotype("WebFault") && ($type.hasStereotype("Exception") || $type.hasStereotype("ApplicationException") || $type.hasStereotype("UnexpectedException")))
#if ($class.name.contains("Exception"))
#set ($className = $class.name.replace("Exception", "Fault"))
#else
#set ($className = "${class.name}Fault")
#end
#else
#set ($className = "$class.name")
#end
#set ($attributes = [])
#if($class.generalization)
import {$class.generalization.name} from '@$class.generalization.filePath';
#end

#foreach ($import in $class.imports)
#if($import.name != $class.name)
import {$import.name} from '@$import.filePath';
#end
#end

#foreach ($tmp in $class.attributes)
#if ($angularUtils.isComplex($tmp))
#if($tmp.type.name != ${class.name})
#set ( $ret = $attributes.add($tmp.type) )
#end
#end
#end
export class $umlUtils.getClassDeclaration($class, $className, $enableTemplating)#if($class.generalization) extends $class.generalization.name#end {

#foreach ($attribute in $class.attributes)
#if(!$attribute.inputButton)
#if($angularUtils.isComplex($attribute) && !$attribute.many && !$angularUtils.isArray($attribute.getterSetterTypeName) && !$attribute.type.enumeration)
    $attribute.name: ${attribute.getterSetterTypeName} | any;
#else
#set ($default = "$attribute.defaultValue")
#if($attribute.many)
#set ($default = "new Array()")
#end 
    $attribute.name: $attribute.getterSetterTypeName | any#if (!$attribute.many && $attribute.defaultValuePresent) = ${attribute.defaultValue}#end;
#end
#end
#end

    constructor() {
#if($class.generalization)
        super();
#end
#foreach ($attribute in $class.attributes)
#if(!$attribute.inputButton)
#if($attribute.many || $attribute.type.listType|| $attribute.type.collectionType|| $attribute.type.arrayType|| $attribute.type.setType)
#set($defaultValue = "[]")
#else
#if (!$attribute.many && $attribute.defaultValuePresent) 
#set($defaultValue = ${attribute.defaultValue})
#else
#set($defaultValue = "null")
#end
#end
        this.$attribute.name = $defaultValue;
#end
#end
    }
}
