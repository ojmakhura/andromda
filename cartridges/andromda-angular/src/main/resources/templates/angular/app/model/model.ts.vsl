#set($destination = $angularHelper.getModelDestination(${class.packageName}))
#set($fileName = $angularHelper.getModelFileName(${class.name}))
export class $class.name {
#foreach ($attribute in $class.attributes)
#set ($returnType = $attribute.getterSetterTypeName)
    $attribute.name: $angularHelper.getAttributeDatatype($returnType);
#end
}
