<?xml version="1.0" encoding="UTF-8"?>
<document xmlns="http://maven.apache.org/XDOC/2.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
          xsi:schemaLocation="http://maven.apache.org/XDOC/2.0 http://maven.apache.org/xsd/xdoc-2.0.xsd">
    <properties>
        <title>
            Create User Entity
        </title>
    </properties>
    <body>
        <section name="Create User Entity">

            <p>
                This page provides directions to create the
                <tt>User</tt>
                entity using ArgoUML.
                The model is shown below for your reference.
            </p>

            <div align="center">
                <img src="images/User.png" alt="User"/>
            </div>
            <p>

                <img src="images/DomainPackage.png" alt="Containment Tree"/>
            </p>
            <p>
                We will create
                <tt>User</tt>
                in a package named <tt>org.andromda.timetracker.domain</tt>.
                When finished, the explorer pane in ArgoUML will look like the screen shot
                on the right.
            </p>
            <ol style="list-style-type: decimal">
                <li>In the TimeTracker model create a package called
                    <tt>domain</tt>
                    under <tt>
                        org.andromda.timetracker</tt>.
                </li>
                <li>In the explorer pane on the left, right-click on the
                    <tt>domain</tt>
                    package
                    and select Create DiagramNew Class Diagram. Type &quot;Domain Objects&quot; as the name of
                    this diagram on the property panel.
                </li>
                <li>Add a new class to the diagram and name it <tt>User</tt>.
                </li>
                <li>Add the stereotype called
                    <tt>Entity</tt>
                    to <tt>User</tt>.
                </li>
                <li>Add the 3 attributes to <tt>User</tt>.
                </li>
                <li>Make sure the User class is selected and go to its property panel at the bottom
                    of the screen.
                </li>
                <li>Click on the
                    <i>Tagged Values</i>
                    tab and add a new tagged value by clicking
                    in the right column and selecting the tagged value named
                    <tt>@andromda.persistence.table</tt>
                    from the drop down menu. This tagged
                    value allows you to override the default table name for the entity. In the
                    left column type the value <tt>USERS</tt>.
                </li>
                <li>Make sure your class diagram matches the one shown above.</li>
                <li>Make sure that the ArgoUML explorer panel matches the screen shot shown above.</li>
                <li>Save the model by selecting FileSave Project.</li>
            </ol>
            <p>
                Now go back to the main tutorial page and continue from where you left off.
            </p>
        </section>
    </body>
</document>
