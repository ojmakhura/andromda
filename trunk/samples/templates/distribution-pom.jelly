<!-- This is the distribution POM (so we can build the samples) -->
<project xmlns:j="jelly:core">
	
<j:whitespace trim="no" xmlns="dummy">
<project>
	<id>andromda-samples</id>
	<groupId>andromda-samples</groupId>
	<name>AndroMDA Samples</name>
	<currentVersion>${pom.currentVersion}</currentVersion>
</project> 	
</j:whitespace>

</project>
