## Only generate the controller factory if there are operations defined
#set ($generatedFile = "${controller.fullPath}Factory.java")
#if (!$controller.operations.empty)
##
##  START TEMPLATE
##
package $controller.packageName;

public class ${controller.name}Factory
{
    private final static ${controller.name} INSTANCE = new ${controller.name}();

    public final static ${controller.name}Interface get${controller.name}Instance()
    {
        return INSTANCE;
    }
}
#end