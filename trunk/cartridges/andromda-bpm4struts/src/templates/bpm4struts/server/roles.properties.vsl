#if ($stringUtils.isNotBlank($securityRealm))
#if(!$applicationUseCases.empty)#set($applicationUseCase=$applicationUseCases.iterator().next())#end
#set ($users = $applicationUseCase.model.allActors)
#
# add your users here and give them roles
# by default the 'admin' user has all roles
#
#set ($separator = '')
admin=#foreach($user in $users)$separator$user.name #set($separator=',')#end
#end