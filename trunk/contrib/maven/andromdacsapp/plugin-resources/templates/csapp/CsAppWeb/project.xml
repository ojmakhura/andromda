<project>
    <extend>../project.xml</extend>
    <name>${application.name}Web</name>
    <artifactId>${pom.groupId}-${application.name}Web</artifactId>
    <shortDescription></shortDescription>
    <description></description>
</project>