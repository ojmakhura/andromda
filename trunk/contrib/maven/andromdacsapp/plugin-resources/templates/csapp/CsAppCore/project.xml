<project>
    <extend>../project.xml</extend>
    <name>${application.name}Core</name>
    <artifactId>${pom.groupId}-${application.name}Core</artifactId>
    <shortDescription></shortDescription>
    <description></description>
</project>