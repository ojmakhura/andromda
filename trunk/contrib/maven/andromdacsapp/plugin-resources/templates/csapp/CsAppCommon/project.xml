<project>
    <extend>../project.xml</extend>
    <name>${application.name}Common</name>
    <artifactId>${pom.groupId}-${application.name}Common</artifactId>
    <shortDescription></shortDescription>
    <description></description>
</project>