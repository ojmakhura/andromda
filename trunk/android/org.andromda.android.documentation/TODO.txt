WICHTIG:

Das Management des Modells geradeziehen. Derzeit gibt es so eine Grauzone zwischen Model (d.h. dem AndroMDA
Configuration Model) und dem Model (also dem Android IModel). Das muss bereinigt werden!

Auﬂerdem: dokumentieren, wie nun die einzelnen Elemente (z.B. der GUI) an die Modelle rankommen: push oder pull?