Template Editor
- Variablen für Code Completion innerhalb eines Jobs laden
- Aktualisierung der Variablenlisten über einen Builder?
- Algorithmus, der Collections und deren Einzelelemente korrekt erkennt
- members: "is" und "get" abschneiden und ZUSÄTZLICH anzeigen
- bold und space werden bei member proposal zu lange gezeigt
- Hilfetext für die Variablen / Members anzeigen (teilweise: JavaDoc, teilweise AndroMDA doc)
- zusaätzliche Templates: foreach (mit unterstützung der aktuellen collection) und if


Cartridge Structure View
- zeigt Innenleben der Cartridge an (die einzelnen Deskriptoren, die Templates etc)
- evtl. als Resource navigator (Top level wären dann alle Projekte, die Cartrdges enthalten oder ein Cartridge root folder sind)

Hilfe
- als xdocs

Editor
- Rausfinden, wie wir am besten mit dem IDocumentProvider umgehen und dafür sorgen, dass der Editor auf einem Document arbeitet. Auf dem Document können wir ja ein DocumentModel aufsetzen!
- Vielleicht ist es tatsächlich am besten, einen rudimentären Editor ganz frisch anzufangen (Artikel!!! :-) )