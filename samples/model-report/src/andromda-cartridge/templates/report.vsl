SIMPLE REPORT
-------------
#foreach ( $modelElement in $transform.modelElements )
#if ($modelElement.name)
ELEMENT = $transform.getFullyQualifiedName($modelElement)
#foreach ( $attribute in $transform.getAttributes($modelElement) )
  attribute $attribute.name
#end
#foreach ( $operation in $transform.getOperations($modelElement) )
  operation $operation.name
#end
#end   
#end