SIMPLE REPORT
-------------
#foreach ( $modelElement in $model.rootPackage.modelElements )
#if ($modelElement.name)
ELEMENT = $modelElement.fullyQualifiedName)
#foreach ( $attribute in $modelElement.attributes)
  attribute $attribute.name
#end
#foreach ( $operation in $modelElement.operations )
  operation $operation.name
#end
#end   
#end