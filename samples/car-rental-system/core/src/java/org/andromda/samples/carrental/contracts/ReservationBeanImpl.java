package org.andromda.samples.carrental.contracts;

import javax.ejb.EntityContext;
import javax.ejb.RemoveException;
//import org.apache.log4j.Logger;

/**
 * The Reservation entity bean.
 *
 * <p>
 *  Represents a reservation for a car with a certain comfort
 *  class, for a given date. @author Matthias Bohlen
 * </p>
 *
 * <p>This class is intended to hold manual changes to the autogenerated
 * implementation in {@link ReservationBean}.</p>
 */
public abstract class ReservationBeanImpl extends ReservationBean {
    //private static Logger log = Logger.getLogger(ReservationBeanImpl.class);
	private EntityContext ctx;
	
	public void setEntityContext(EntityContext ctx) {
        //if(log.isDebugEnabled()) log.debug("ReservationBean.setEntityContext...");
        this.ctx = ctx;
    }

    public void unsetEntityContext() {
        //if(log.isDebugEnabled()) log.debug("ReservationBean.unsetEntityContext...");
        ctx = null;
    }

    public void ejbRemove() throws RemoveException {
        //if(log.isDebugEnabled()) log.debug("ReservationBean.ejbRemove...");
    }

    public void ejbLoad() {
        //if(log.isDebugEnabled()) log.debug("ReservationBean.ejbLoad...");
    }

    public void ejbStore() {
        //if(log.isDebugEnabled()) log.debug("ReservationBean.ejbStore...");
    }

    public void ejbPassivate() {
        //if(log.isDebugEnabled()) log.debug("ReservationBean.ejbPassivate...");
    }

    public void ejbActivate() {
        //if(log.isDebugEnabled()) log.debug("ReservationBean.ejbActivate...");
    }

    /**
     * 
     */
    public org.andromda.samples.carrental.contracts.Reservation create(long id, java.util.Date reservationDate, java.lang.String comfortClass) {
        // TODO put your implementation here.
        Reservation result = null;
        return result;
    }

}
